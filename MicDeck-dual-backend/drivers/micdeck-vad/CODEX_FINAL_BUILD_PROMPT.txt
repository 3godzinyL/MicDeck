You are finishing MicDeck VAD on a Windows 11 disposable driver-test machine.

Do not claim success from source inspection.

Required order:

1. Read:
   - docs/THREE_FINAL_UPDATES.md
   - docs/CERTIFICATION_LEVELS.md
   - docs/KNOWN_GAPS.md
   - docs/MICDECK_INTEGRATION_COMPLETE.md

2. Run the portable tests.

3. Run:
   scripts/bootstrap-official-reference.ps1 -Build

   The pinned Microsoft reference must build before diagnosing custom WDK
   errors.

4. Build MicDeckVad.sln with the installed Visual Studio/SDK/WDK.

5. Correct all actual WDK contract, PortCls table, power-management and INF
   errors. Do not remove fail-closed validation, tests or security checks.

6. Run:
   scripts/full-wdk-certification-gate.ps1 -Configuration Debug -TestSign

7. Do not report "working" unless certification-report.json shows all requested
   gates passed and audio-e2e.json reports passed=true.

8. Configure Driver Verifier only on the disposable target:
   verifier /standard /driver MicDeckVad.sys
   verifier /bootmode oneboot

9. After reboot, run:
   - repeated stream open/close;
   - audiosrv restart;
   - driver disable/enable;
   - sleep/resume;
   - 24-hour soak.

10. Integrate with MicDeck only after the isolated render-to-capture certifier
    passes. Preserve the existing native DSP/mixer and replace only the selected
    VB-CABLE render endpoint.

Produce:
- exact build errors fixed;
- source diff;
- certification-report.json;
- audio-e2e.json;
- verifier result;
- remaining production-signing/HLK work.

Never disable tests, fake endpoint checks or mark skipped stages as passed.
